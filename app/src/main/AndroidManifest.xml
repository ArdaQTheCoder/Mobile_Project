<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".AutoServiceApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mobile_Project">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Auth -->
        <activity android:name=".ui.auth.LoginActivity" />
        <activity android:name=".ui.auth.RegisterActivity" />

        <!-- Customer -->
        <activity android:name=".ui.customer.CustomerMainActivity" />
        <activity android:name=".ui.customer.VehicleListActivity" />
        <activity android:name=".ui.customer.AddVehicleActivity" />
        <activity android:name=".ui.customer.MechanicListActivity" />
        <activity android:name=".ui.customer.MechanicDetailActivity" />
        <activity android:name=".ui.customer.BookAppointmentActivity" />
        <activity android:name=".ui.customer.AppointmentListActivity" />
        <activity android:name=".ui.customer.ServiceRecordsActivity" />
        <activity android:name=".ui.customer.RateAppointmentActivity" />

        <!-- Mechanic -->
        <activity android:name=".ui.mechanic.MechanicMainActivity" />
        <activity android:name=".ui.mechanic.MechanicPendingActivity" />
        <activity android:name=".ui.mechanic.MechanicAppointmentDetailActivity" />
        <activity android:name=".ui.mechanic.MechanicAllAppointmentsActivity" />

        <!-- Common -->
        <activity android:name=".ui.common.ChatListActivity" />
        <activity android:name=".ui.common.ChatActivity" />
        <activity android:name=".ui.common.SettingsActivity" />

    </application>

</manifest>
